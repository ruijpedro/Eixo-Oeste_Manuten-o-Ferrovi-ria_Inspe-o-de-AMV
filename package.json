{
  "scripts": {
    "start": "npx http-server .",
    "build": "mkdir -p dist && cp -r index.html manifest.webmanifest service-worker.js assets src dist/",
    "preview": "npx http-server dist"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "typescript": "^5.6.3"
  }
}